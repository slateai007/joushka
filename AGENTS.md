# Repository Workflow

- Treat `slateai007/joushka` as the primary working repository.
- Never include `codex` in branch names.
- Prefix every pull request title with `[Haseeb]`.
- After `[Haseeb]`, format pull request titles as `feat/xyz`, `fix/xyz`, or `update/xyz`.
- Open pull requests as ready for review, never as drafts.
- Submit completed changes through pull requests targeting `main`.
